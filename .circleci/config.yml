# This config was automatically generated from your source code
# Stacks detected: deps:python:.
version: 2.1
orbs:
  python: circleci/python@2
jobs:
  test-python:
    # Install dependencies and run tests
    docker:
      - image: cimg/python:3.8
    steps:
      - checkout
      - run:
          name: install packages
          command: |
              python3 -m venv venv
	      . venv/bin/activate
              pip install pip-tools
              pip install pytest pytest-cov
              pip-compile
              pip-sync
      - run:
          name: Run tests
          command: pytest --cov=ncjson --cov-report=html
      #- store_test_results:
      #    path: junit.xml
workflows:
  build-and-test:
    jobs:
      - test-python
